plugins {
	id 'base'
}

allprojects {
	version = project.mod_version
	group = project.maven_group

	repositories {
		mavenCentral()
	}
}

tasks.named('build') {
	dependsOn ':common:build', ':fabric-1.20.1:build', ':fabric-1.21.1:build'
}
